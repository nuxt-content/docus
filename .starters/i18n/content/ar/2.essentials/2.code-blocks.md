---
title: كتل الكود
description: اعرض الكود المضمّن وكتل الكود داخل توثيقك.
links: []
navigation:
  icon: i-lucide-code-xml
---

## الأساسيات

### الكود المضمّن

استخدم الكود المضمّن لعرض مقاطع كود قصيرة داخل الفقرات. هذا مناسب للإشارة إلى عناصر الكود مباشرة داخل الجملة.

::code-preview
---
class: "[&>div]:*:my-0"
---
`كود مضمّن`

#code
```mdc
`كود مضمّن`
```
::

### كتل الكود

استخدم كتل الكود لعرض مقاطع كود متعددة الأسطر مع تلوين الصيغة. كتل الكود أساسية لعرض أمثلة الكود بوضوح.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
---
```ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```

#code
````mdc
```ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```
````
::

عند كتابة كتلة كود، يمكنك تحديد اسم ملف يظهر أعلى الكتلة. ستظهر أيقونة تلقائيًا حسب الامتداد أو الاسم.
تساعد أسماء الملفات المستخدمين على فهم مكان الكود وهدفه داخل المشروع.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
---
```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```

#code
````mdc
```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```
````
::

كل كتلة كود تحتوي على زر نسخ مدمج يسمح بنسخ الكود إلى الحافظة.

::tip{to="https://ui.nuxt.com/getting-started/icons/nuxt#theme"}
الأيقونات معرفة افتراضيًا، لكن يمكنك تخصيصها داخل `app.config.ts`:

```ts [app.config.ts]
export default defineAppConfig({
  ui: {
    prose: {
      codeIcon: {
        terminal: 'i-ph-terminal-window-duotone'
      }
    }
  }
})
```
::

## متقدم

### CodeGroup

اجمع عدة كتل كود داخل تبويبات باستخدام `code-group`. هذا مناسب لعرض أمثلة بعدة لغات أو عدة مديري حزم.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
---
  :::code-group{.w-full}
  ```bash [pnpm]
  pnpm add @nuxt/ui
  ```

  ```bash [yarn]
  yarn add @nuxt/ui
  ```

  ```bash [npm]
  npm install @nuxt/ui
  ```

  ```bash [bun]
  bun add @nuxt/ui
  ```
  :::

#code
````mdc
::code-group

```bash [pnpm]
pnpm add @nuxt/ui
```

```bash [yarn]
yarn add @nuxt/ui
```

```bash [npm]
npm install @nuxt/ui
```

```bash [bun]
bun add @nuxt/ui
```

::
````
::

### CodeTree

اعرض كتل الكود في شكل شجرة باستخدام `code-tree`. هذا ممتاز لعرض بنية المشاريع والعلاقات بين الملفات.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
---
  :::code-tree{default-value="app/app.config.ts"}
  ```ts [nuxt.config.ts]
  export default defineNuxtConfig({
    modules: ['@nuxt/ui'],

    css: ['~/assets/css/main.css']
  })
  ```

  ```css [app/assets/css/main.css]
  @import "tailwindcss";
  @import "@nuxt/ui";
  ```

  ```ts [app/app.config.ts]
  export default defineAppConfig({
    ui: {
      colors: {
        primary: 'sky',
        colors: 'slate'
      }
    }
  })
  ```

  ```vue [app/app.vue]
  <template>
    <UApp>
      <NuxtPage />
    </UApp>
  </template>
  ```

  ```json [package.json]
  {
    "name": "nuxt-app",
    "private": true,
    "type": "module",
    "scripts": {
      "build": "nuxt build",
      "dev": "nuxt dev",
      "generate": "nuxt generate",
      "preview": "nuxt preview",
      "postinstall": "nuxt prepare",
      "lint": "eslint .",
      "lint:fix": "eslint --fix ."
    },
    "dependencies": {
      "@iconify-json/lucide": "^1.2.18",
      "@nuxt/ui": "^4.0.0",
      "nuxt": "^4.1.0"
    },
    "devDependencies": {
      "eslint": "^9.34.0",
      "typescript": "^5.9.3",
      "vue-tsc": "^3.0.6"
    }
  }
  ```

  ```json [tsconfig.json]
  {
    "extends": "./.nuxt/tsconfig.json"
  }
  ```

  ````md [README.md]
  # Nuxt 4 Minimal Starter

  راجع [توثيق Nuxt 4](https://nuxt.com/docs/getting-started/introduction) لمعرفة المزيد.

  ## التثبيت

  تأكد من تثبيت الاعتماديات:

  ```bash
  # npm
  npm install

  # pnpm
  pnpm install

  # yarn
  yarn install

  # bun
  bun install
  ```

  ## خادم التطوير

  شغّل خادم التطوير على `http://localhost:3000`:

  ```bash
  # npm
  npm run dev

  # pnpm
  pnpm run dev

  # yarn
  yarn dev

  # bun
  bun run dev
  ```

  ## الإنتاج

  ابنِ التطبيق للإنتاج:

  ```bash
  # npm
  npm run build

  # pnpm
  pnpm run build

  # yarn
  yarn build

  # bun
  bun run build
  ```

  عاين نسخة الإنتاج محليًا:

  ```bash
  # npm
  npm run preview

  # pnpm
  pnpm run preview

  # yarn
  yarn preview

  # bun
  bun run preview
  ```

  راجع [توثيق النشر](https://nuxt.com/docs/getting-started/deployment) لمزيد من المعلومات.
  ````
  :::
::

### `CodePreview`

استخدم `code-preview` لعرض نتيجة الكود بجانب الكود نفسه. هذا مناسب للأمثلة التفاعلية وشرح نتائج الكود.
اكتب الكود المراد معاينته في فتحة `default` والكود الفعلي في فتحة `code`.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
label: معاينة
---
  :::code-preview
  ---
  class: "[&>div]:*:my-0"
  ---
  `كود مضمّن`

  #code
  ```mdc
  `كود مضمّن`
  ```
  :::

#code
````mdc
::code-preview
`كود مضمّن`

#code
```mdc
`كود مضمّن`
```
::
````
::

### `CodeCollapse`

استخدم `code-collapse` مع كتل الكود الطويلة للحفاظ على نظافة الصفحات. يتيح للمستخدمين فتح الكود الطويل عند الحاجة فقط، مما يحسّن قابلية القراءة.

::code-preview
---
class: "[&>div]:*:my-0 [&>div]:*:w-full"
---
  :::code-collapse
  ---
  class: "[&>div]:my-0"
  ---
  ```css [main.css]
  @import "tailwindcss";
  @import "@nuxt/ui";

  @theme {
    --font-sans: 'Public Sans', sans-serif;

    --breakpoint-3xl: 1920px;

    --color-green-50: #EFFDF5;
    --color-green-100: #D9FBE8;
    --color-green-200: #B3F5D1;
    --color-green-300: #75EDAE;
    --color-green-400: #00DC82;
    --color-green-500: #00C16A;
    --color-green-600: #00A155;
    --color-green-700: #007F45;
    --color-green-800: #016538;
    --color-green-900: #0A5331;
    --color-green-950: #052E16;
  }
  ```
  :::

#code
````mdc
::code-collapse

```css [main.css]
@import "tailwindcss";
@import "@nuxt/ui";

@theme {
--font-sans: 'Public Sans', sans-serif;

--breakpoint-3xl: 1920px;

--color-green-50: #EFFDF5;
--color-green-100: #D9FBE8;
--color-green-200: #B3F5D1;
--color-green-300: #75EDAE;
--color-green-400: #00DC82;
--color-green-500: #00C16A;
--color-green-600: #00A155;
--color-green-700: #007F45;
--color-green-800: #016538;
--color-green-900: #0A5331;
--color-green-950: #052E16;
}
```

::
````
::
